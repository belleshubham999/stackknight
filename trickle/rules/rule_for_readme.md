When updating the project:
- Always check if the `trickle/notes/README.md` needs to be updated to reflect new features, file structure changes, or gameplay mechanics.
- Keep the Tech Stack section up to date if new libraries are added.